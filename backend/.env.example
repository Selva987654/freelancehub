# Copy this file to .env and adjust if needed. Sensible defaults are already used if .env is absent.

PORT=4000
JWT_SECRET=freelancerhub-demo-secret-change-in-production
JWT_EXPIRES_IN=7d
DB_PATH=./data/freelancerhub.db
CORS_ORIGIN=http://localhost:5173
UPLOAD_DIR=./uploads

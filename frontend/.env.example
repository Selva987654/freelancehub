# The frontend talks to the backend through the Vite dev/preview proxy by default,
# so no configuration is required for local development.
#
# Only set this if you deploy the frontend separately from the backend and need
# to point it at an absolute API origin.
# VITE_API_BASE_URL=http://localhost:4000
